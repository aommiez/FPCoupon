{% extends "layout.twig" %}

{% block body %}
    <style>
        .cp {
            border: 1px dotted #ccccd7;
            margin-bottom: 6px;
            padding: 6px;
        }
    </style>
    <br>
    <div class="container" style="background-color: #ffffff;text-align: center;width: 340px;">
        <h4>Campaign Name</h4>
        <h5>15-11-2018 ถึง 25-11-2018</h5>
        <p>
            คุณได้กรอกข้อมูลเรียบร้อยแล้วเป็นจำนวน
        </p>
        <div style="border: 2px solid #595fde;margin: 20px 100px 20px 100px;font-size: 20px;">
            {{ coupons|length }} ใบ
        </div>
        <div>
            {% for cp in coupons %}
                <p class="print text-left cp">
                    Coupon ID : {{ cp.coupon_id|e }}<br>
                    First Name : {{ cp.FirstName }}<br>
                    last Name : {{ cp.LastName }}<br>
                    ID Card : {{ cp.pers_idcard }}<br>
                    Phone Number : {{ cp.Pers_PhoneNumber }}
                </p>
            {% endfor %}
        </div>
        <p>
            กรุณาติดต่อพิมพ์คูปองได้ที่จุดแลกคูปอง
        </p>
        <p>
           <a href="#">เงื่อนไขการลุ้นรางวัลชิงโชค</a>
        </p>
    </div>
    <br>
    <br>
    <div class="container">
        <button type="button" class="btn btn-block" onclick="goBackEdit()">กลับไปแก้ไขข้อมูล</button>
        <button type="button" class="btn btn-block" onclick="goBack()">กลับหน้าแรก</button>
    </div>
{% endblock %}

{% block script %}
    <script type="text/javascript">
        function goBack() {
            window.location.replace("{{ base_url() }}");
        }

        function goBackEdit() {
            window.history.back();
        }

        $(function () {
            $("#printBtn").click(function () {
                $('#p').printThis();
            });
        });
    </script>
{% endblock %}
